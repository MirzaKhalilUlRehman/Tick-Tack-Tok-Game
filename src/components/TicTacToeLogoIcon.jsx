import React from 'react';
import TicTacToeLogo from './common/TicTacToeLogo';

export default function TicTacToeLogoIcon({ className = 'w-10 h-10' }) {
  return <TicTacToeLogo className={className} />;
}
